export default function Loading({ message }) {
  return (
    <section className="news-app__loading loading-indicator">
      <div className="loading-indicator__title">Loading...</div>
    </section>
  );
}